# ResumeIQ Shared Package

Shared TypeScript contracts and utilities used by the frontend foundation.

Current scope:

- platform metadata contracts
- dependency health types

This package is intentionally small in the foundation phase and can grow as frontend modules begin sharing more typed contracts.
